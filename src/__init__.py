"""
Imports: audio feature extraction, VAE, clustering, evaluation
"""
import sys
import os

# Add src to path so we can import modules
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', 'src'))
